function sample3()
{
    sample_btn_active("sample3");
    app.only_table_reset();

    // SPN
    app.process_add_by_param(0, 7, 0);
    app.process_add_by_param(2, 4, 0);
    app.process_add_by_param(4, 2, 0);
    app.process_add_by_param(5, 1, 0);
    
}